// Unit Test For File
import { describe, expect, test } from '@jest/globals';
import { reverse } from './reverse_string_day1';

describe('Reverse String Function', () => {
  test('Makes sure value is reverse', () => {
    expect(reverse('ABC')).toBe('CBA');
  });

  test;
});
